[package]
name = "sio2-core"
version = "0.1.0"
edition = "2024"

[features]
alloc = []
std = ["alloc"]
indexmap = ["dep:indexmap", "alloc"]

[dependencies]
thiserror = { workspace = true }
indexmap = { workspace = true, optional = true }
